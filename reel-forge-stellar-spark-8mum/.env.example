# ReelForge AI — Environment Variables

# Our own CrewAI agent server (replaces Lyzr)
AGENT_SERVER_URL=http://localhost:8000

# Our own video pipeline server
VIDEO_PIPELINE_URL=http://localhost:8001

# Legacy Lyzr (no longer needed — kept for reference)
# LYZR_API_KEY=not-needed-anymore
